import React from 'react';

const Contacts= () => {
    return (
        <div className="promo">
            Контакты
        </div>
    );
};

Contacts.propTypes = {};

Contacts.displayName = 'Contacts';

export default Contacts;
