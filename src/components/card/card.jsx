import React from 'react';

const Card = () => {
    return (
        <div className="promo">
            Карточка товара
        </div>
    );
};

Card.propTypes = {};

Card.displayName = 'Card';

export default Card;
